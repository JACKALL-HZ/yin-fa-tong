# FastAPI Core
fastapi==0.111.0
uvicorn[standard]==0.30.1
pydantic==2.7.4
pydantic-settings==2.3.4

# Database
sqlalchemy[asyncio]==2.0.31
aiomysql==0.2.0
alembic==1.13.1

# Auth
python-jose[cryptography]==3.3.0
bcrypt==4.1.3
python-multipart==0.0.9

# Cache & Queue
redis==5.0.7
aio-pika==9.4.2

# Search
elasticsearch[async]==8.14.0

# LangGraph 导诊编排（自建图，Chroma 向量检索）
langgraph>=1.0.0
langgraph-checkpoint-sqlite>=2.0.0
langchain-core>=0.2.0
# openai 仅作 OpenAI-compatible 客户端（可配 DeepSeek/通义/硅基流动 base_url）
openai>=1.30.0
# Chroma 向量库（知识库语义检索，embedding 走硅基流动 API，不装本地模型）
chromadb>=1.0.0
# BM25 稀疏检索（混合检索一路，纯 Python 轻量，不依赖本地模型）
rank-bm25>=0.2.2

# Scheduler
apscheduler==3.10.4

# Image Processing
opencv-python==4.10.0.84
pytesseract==0.3.10

# Payment - Alipay
alipay-sdk-python==3.7.33

# SMS - Alibaba Cloud (号码认证服务)
alibabacloud-dypnsapi20170525>=2.0.0
alibabacloud-tea-openapi>=0.3.0

# HTTP client（API embedding 调用）
httpx==0.27.0

# note: 本地 BGE-M3 的 FlagEmbedding/modelscope/torch 在此镜像中不安装，
#       服务器低配(2C4G)改用硅基流动 BGE-M3 API（EMBEDDING_PROVIDER=api）。
